"""Módulo de lectura de configuración del pipeline.

Carga configuración desde config/config.yaml y variables de entorno (.env).
Centraliza acceso a parámetros de conexión (BigQuery, Cloud Firestore) y
configuración del pipeline ETL.
"""
